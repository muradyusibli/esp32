#pragma once

#include <Arduino.h>

// Rename this file to config.h and fill in your real values.
// Keep config.h out of git because it contains WiFi credentials.

const char* ssid     = "ucll-integratieproject";
const char* password = "aesheiSei8uaMiag";
const char* host     = "10.25.249.253";  // backend LAN IP
const uint16_t port  = 3000;
const String roomId  = "Connect";
